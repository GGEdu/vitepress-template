import { defineConfig } from 'vitepress'

// https://vitepress.vuejs.org/config/app-configs
export default defineConfig({
  // base: '/', // Usa esto para desarrollo local
  base: '/vitepress-template/', // Cambia a '/<REPO_NAME>/' para despliegue en GitHub Pages
})
